@model data_api.Models.Cliente

@{
    ViewBag.Title = "Gestion de Clientes";
}

<div class="form-horizontal">
    <h4>Cliente</h4>
    <hr />
    <form class="m-t" role="form" action="@Url.Action("GuardarCliente", "Cliente")" method="post">
        <div class="form-group">
            <input id="User" type="text" class="form-control" placeholder="nombre..." required="" name="nombre">
        </div>
        <div class="form-group">
            <input id="Pass" type="text" class="form-control" placeholder="apellido..." required="" name="apellido">
        </div>
        <div class="form-group">
            <input id="Pass" type="text" class="form-control" placeholder="cc..." required="" name="cc">
        </div>
        <button type="submit" class="btn btn-primary block full-width m-b">Guardar</button>
        @*<a href="#"><small>Forgot password?</small></a>
            <p class="text-muted text-center"><small>Do not have an account?</small></p>
            <a class="btn btn-sm btn-white btn-block" href="@Url.Action("Register", "Pages")">Create an account</a>*@
    </form>
</div>


